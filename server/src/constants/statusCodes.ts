const queryError = 500; // Internal Server Error
const notFound = 404; // Not Found
const badGateway = 502; // Bad Gateway
const success = 200; // OK
const created = 201; // Created
const accepted = 202; // Accepted
const noContent = 204; // No Content
const unauthorized = 401; // Unauthorized
const forbidden = 403; // Forbidden
const badRequest = 400; // Bad Request
const userAlreadyExists = 409; // Conflict
const internalServerError = 500; // Internal Server Error
const serviceUnavailable = 503; // Service Unavailable

export default {
  queryError,
  notFound,
  badGateway,
  success,
  created,
  accepted,
  noContent,
  unauthorized,
  forbidden,
  badRequest,
  userAlreadyExists,
  internalServerError,
  serviceUnavailable,
};
